---
phase: 02-type-safety
plan: 02-05
type: execute
wave: 4
depends_on: [02-01]
files_modified:
  - src/features/employee-crud/components/WorkerTrainingDownloadButton.tsx
  - src/features/permits/components/PermitStatusBadge.tsx
  - src/features/permits/components/PermitActions.tsx
  - src/features/permits/components/PermitsTable.tsx
  - src/features/orders/components/OrdersTable.tsx
  - src/features/orders/components/ResponsiblePersonMultiSelect.tsx
  - src/features/orders/components/OrderForm.tsx
  - src/features/permits/components/PermitForm.tsx
  - src/features/permits/components/PermitsDashboard.tsx
  - src/features/prescriptions/components/ResponsiblePersonSelect.tsx
  - src/features/prescriptions/components/PrescriptionsTable.tsx
autonomous: true
requirements: [TYPE-01, TYPE-02]
must_haves:
  truths:
    - "All 11 .jsx files are renamed to .tsx"
    - "npx tsc --noEmit exits 0 after each file conversion — 0 errors throughout"
    - "No as any introduced in any converted file"
    - "memo-wrapped components use memo<Props>(Component) pattern preserving prop types"
    - "Inline sub-components in PermitsDashboard and PrescriptionsTable have explicit React.ReactNode children"
    - "Explicit .jsx extension in PrescriptionForm is updated when ResponsiblePersonSelect is renamed"
  artifacts:
    - path: "src/features/employee-crud/components/WorkerTrainingDownloadButton.tsx"
      provides: "Typed TSX with WorkerTrainingDownloadButtonProps interface"
      contains: "interface WorkerTrainingDownloadButtonProps"
    - path: "src/features/permits/components/PermitStatusBadge.tsx"
      provides: "Typed TSX with PermitStatusBadgeProps interface"
      contains: "interface PermitStatusBadgeProps"
    - path: "src/features/permits/components/PermitActions.tsx"
      provides: "Typed TSX with PermitActionsProps interface and union state type"
      contains: "confirmingAction"
    - path: "src/features/permits/components/PermitsTable.tsx"
      provides: "Typed TSX importing PermitStatusBadge.tsx and PermitActions.tsx"
      contains: "interface PermitsTableProps"
    - path: "src/features/orders/components/OrdersTable.tsx"
      provides: "Typed TSX with memo<OrdersTableProps> pattern"
      contains: "memo<OrdersTableProps>"
    - path: "src/features/orders/components/ResponsiblePersonMultiSelect.tsx"
      provides: "Typed TSX with useRef generics"
      contains: "useRef<HTMLDivElement>"
    - path: "src/features/orders/components/OrderForm.tsx"
      provides: "Typed TSX importing ResponsiblePersonMultiSelect.tsx"
      contains: "interface OrderFormProps"
    - path: "src/features/permits/components/PermitForm.tsx"
      provides: "Typed TSX with PermitFormProps interface"
      contains: "interface PermitFormProps"
    - path: "src/features/permits/components/PermitsDashboard.tsx"
      provides: "Typed TSX with inline sub-components typed with React.ReactNode children"
      contains: "React.ReactNode"
    - path: "src/features/prescriptions/components/ResponsiblePersonSelect.tsx"
      provides: "Typed TSX with useRef generics and memo<Props> pattern"
      contains: "useRef<HTMLDivElement>"
    - path: "src/features/prescriptions/components/PrescriptionsTable.tsx"
      provides: "Typed TSX with typed inline sub-components"
      contains: "interface PrescriptionsTableProps"
  key_links:
    - from: "src/features/permits/components/PermitsTable.tsx"
      to: "src/features/permits/components/PermitStatusBadge.tsx"
      via: "import — must be converted first"
      pattern: "from.*PermitStatusBadge"
    - from: "src/features/permits/components/PermitsTable.tsx"
      to: "src/features/permits/components/PermitActions.tsx"
      via: "import — must be converted first"
      pattern: "from.*PermitActions"
    - from: "src/features/orders/components/OrderForm.tsx"
      to: "src/features/orders/components/ResponsiblePersonMultiSelect.tsx"
      via: "import — must be converted first"
      pattern: "from.*ResponsiblePersonMultiSelect"
    - from: "src/features/prescriptions/components/PrescriptionForm.jsx"
      to: "src/features/prescriptions/components/ResponsiblePersonSelect.tsx"
      via: "explicit .jsx extension import — update when renaming ResponsiblePersonSelect"
      pattern: "from.*ResponsiblePersonSelect\\.jsx"
---

<objective>
Convert 11 .jsx files to strict .tsx in dependency-safe order: leaf components first, then components that import them. Each conversion adds a typed Props interface, no `as any`, and passes `npx tsc --noEmit` 0 errors before proceeding to the next file.

Purpose: Partial completion of TYPE-01 and TYPE-02. The 11 files in this plan are leaves and intermediate components. The 5 registry-level files (the highest-complexity group) are handled in plan 02-06.

Output: 11 renamed .tsx files each with a Props interface, memo generic typing where applicable, and correct useRef generics.
</objective>

<execution_context>
@/root/projects/react/BuildingPersonalApp/.claude/get-shit-done/workflows/execute-plan.md
@/root/projects/react/BuildingPersonalApp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-type-safety/02-CONTEXT.md
@.planning/phases/02-type-safety/02-RESEARCH.md
@CLAUDE.md
@src/features/employee-crud/components/EmployeeTable.tsx
@src/features/tasks/services/tasksService.ts
</context>

<interfaces>
Reference TSX component pattern (from EmployeeTable.tsx):
```typescript
// 1. Props interface above component
interface ComponentProps {
  items: Item[];
  onEdit: (item: Item) => void;
  onDelete: (id: string) => void;
}

// 2. Typed function component
function Component({ items = [], onEdit, onDelete }: ComponentProps) {
  // ...
}

// 3. memo with explicit generic (preserves prop types)
export default memo<ComponentProps>(Component);
```

AddNotificationFn type (inline — do NOT import from app/ layer):
```typescript
type AddNotificationFn = (message: string, type?: string, duration?: number) => void;
```

useRef generic pattern:
```typescript
const containerRef = useRef<HTMLDivElement>(null);
const inputRef = useRef<HTMLInputElement>(null);
```

useDeferredValue generic:
```typescript
const deferred = useDeferredValue<string>(value);
```

Inline sub-component pattern (for PermitsDashboard Card, CardHeader, etc.):
```typescript
type CardProps = { children: React.ReactNode; className?: string };
function Card({ children, className }: CardProps) { ... }
```

union state type pattern (for PermitActions):
```typescript
const [confirmingAction, setConfirmingAction] = useState<'extend' | 'close' | null>(null);
```

recharts CustomTooltip pattern (for PermitsDashboard):
```typescript
import type { TooltipProps } from 'recharts';
// If TooltipProps doesn't import cleanly from 'recharts', use:
// type CustomTooltipProps = { active?: boolean; payload?: Array<{ value: unknown; name: string }> };
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert 6 leaf and table components (files 1–6 of 11)</name>
  <files>
    src/features/employee-crud/components/WorkerTrainingDownloadButton.tsx,
    src/features/permits/components/PermitStatusBadge.tsx,
    src/features/permits/components/PermitActions.tsx,
    src/features/permits/components/PermitsTable.tsx,
    src/features/orders/components/OrdersTable.tsx,
    src/features/prescriptions/components/PrescriptionsTable.tsx
  </files>
  <action>
Convert files in this exact order. After each rename, run `npx tsc --noEmit` and fix all errors before proceeding to the next file.

**File 1: WorkerTrainingDownloadButton.jsx → .tsx** (48 lines)

Read the file. Add above the component:
```typescript
type AddNotificationFn = (message: string, type?: string, duration?: number) => void;

interface WorkerTrainingDownloadButtonProps {
  workerId: string;
  workerName: string;
  addNotification: AddNotificationFn;
}
```
Rename file to `.tsx`. Type the component parameter. Run `npx tsc --noEmit` → 0 errors.

**File 2: PermitStatusBadge.jsx → .tsx** (51 lines)

Read the file. Add above the component:
```typescript
import type { Permit } from '@/entities/permit';

interface PermitStatusBadgeProps {
  permit: Partial<Permit>;
  showDays?: boolean;
  statusOverride?: string;
}
```
Rename to `.tsx`. Type the component. Run `npx tsc --noEmit` → 0 errors.

**File 3: PermitActions.jsx → .tsx** (385 lines) — convert BEFORE PermitsTable

Read the file fully. Key typing work:
- Props interface: 6 props — read the JSX file to identify exact prop names and types
- Union state: `useState<'extend' | 'close' | null>(null)` for the confirming action state
- Internal `buildStatusCandidates` function: add `(kind: 'close' | 'extend'): string[]` return type
- Async audit chain: async functions already return Promise implicitly; add explicit return types if tsc requires them
- `addNotification: AddNotificationFn` in props

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 4: PermitsTable.jsx → .tsx** (271 lines) — AFTER PermitStatusBadge and PermitActions

Read the file. This imports PermitStatusBadge and PermitActions (now both .tsx). The barrel index.ts uses extension-free imports so no barrel changes needed.

Props interface: 6 props — identify from JSX file. The `toggleSort` callback should be typed as `(field: string) => void` internally.

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 5: OrdersTable.jsx → .tsx** (350 lines) — standalone, no cross-file deps

Read the file. Key typing work:
- Props: `orders: Order[]`, `onEdit: (order: Order) => void`, `onDelete: (id: string) => void`, `hasFilters?: boolean`
- Import Order type: `import type { Order } from '@/entities/order'` (check the entity barrel for the correct type export)
- `memo` wrapping: `export default memo<OrdersTableProps>(OrdersTable)` (NOT `memo(OrdersTable)`)
- Internal helpers `getComparableValue`, `compareOrders`: add parameter types
- `collator`: `const collator: Intl.Collator = new Intl.Collator(...)` — TypeScript infers this, just verify no errors
- `useState<string | null>(null)` for `confirmingId`

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 6: PrescriptionsTable.jsx → .tsx** (479 lines) — standalone

Read the file fully. Key typing work:
- Props interface: 4 props — identify from JSX
- Two inline sub-components (`StatusBadge`, `DeadlineCell`): each needs typed props
  - Check `@/entities/prescription` for PrescriptionStatusValue type
  - `type StatusBadgeProps = { status: string }` (or use the specific status type if exported)
  - `type DeadlineCellProps = { deadline: string | null }` (or whatever the actual prop is)
- `memo<PrescriptionsTableProps>` on the export

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

After all 6 files: run `npx tsc --noEmit` one final time to confirm 0 errors across all conversions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    All 6 files exist as .tsx (not .jsx).
    `npx tsc --noEmit` exits 0.
    `grep -rn "as any" src/features/permits/components/ src/features/orders/components/ src/features/prescriptions/components/ src/features/employee-crud/components/` returns no output for the 6 converted files.
    No original .jsx files remain for these 6 names.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert 5 select/form/dashboard components (files 7–11 of 11)</name>
  <files>
    src/features/orders/components/ResponsiblePersonMultiSelect.tsx,
    src/features/orders/components/OrderForm.tsx,
    src/features/permits/components/PermitForm.tsx,
    src/features/permits/components/PermitsDashboard.tsx,
    src/features/prescriptions/components/ResponsiblePersonSelect.tsx
  </files>
  <action>
Convert files in this exact order. After each rename, run `npx tsc --noEmit` → fix all errors → proceed.

**File 7: ResponsiblePersonMultiSelect.jsx → .tsx** (357 lines) — BEFORE OrderForm

Read the file. Key typing work:
- 7-prop interface: identify all props from JSX file; note `value: string[]`, `onChange: (ids: string[]) => void`, `employees: RegistryEmployee[]`
- `RegistryEmployee` type: import from `@/features/orders/services/ordersService` (this is where the type is defined per RESEARCH.md)
- `useRef<HTMLDivElement>(null)` and `useRef<HTMLInputElement>(null)`
- `useDeferredValue<string>(searchTerm)` 
- `memo<ResponsiblePersonMultiSelectProps>` on the export
- `addNotification?: AddNotificationFn` — check if this prop exists in the JSX; it may not be present

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 8: OrderForm.jsx → .tsx** (516 lines) — AFTER ResponsiblePersonMultiSelect

Read the file. Key typing work:
- 5-prop interface: identify from JSX
- `RegistryEmployee` type import from `@/features/orders/services/ordersService`
- `addNotification: AddNotificationFn` in props
- `useId()` is already typed in React 18 — no manual typing needed
- The import of `ResponsiblePersonMultiSelect` is extension-free in the JSX file (verify with grep) — if it has no explicit extension, no import update needed

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 9: PermitForm.jsx → .tsx** (249 lines)

Read the file. Key typing work:
- 6-prop interface: identify from JSX
- `Permit`, `PermitInsert` types from `@/entities/permit`
- `RegistryEmployee` type from `@/features/permits/services/permitsService`

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 10: PermitsDashboard.jsx → .tsx** (180 lines)

Read the file fully. Key typing work — this is the most complex in this batch:
- 2-prop interface: identify from JSX
- 4 inline sub-components: `Card`, `CardHeader`, `CardTitle`, `CardContent`
  - Each needs: `type CardProps = { children: React.ReactNode; className?: string }`
  - Use `React.PropsWithChildren<{ className?: string }>` as an alternative
- recharts `CustomTooltip`: try `import type { TooltipProps } from 'recharts'` first
  - If TypeScript errors: fall back to `type CustomTooltipProps = { active?: boolean; payload?: Array<{ value: unknown; name: string; dataKey: string }> }`
  - Do NOT use `as any` — use the fallback inline type if recharts types are problematic

Rename to `.tsx`. Run `npx tsc --noEmit` → 0 errors.

**File 11: ResponsiblePersonSelect.jsx → .tsx** (329 lines) — single-select variant

Read the file. Key typing work:
- 7-prop interface: similar to ResponsiblePersonMultiSelect but single-select (`value: string | null`, `onChange: (id: string | null) => void`)
- `RegistryEmployee` type from `@/features/prescriptions/services/prescriptionsService`
- `useRef<HTMLDivElement>(null)` and `useRef<HTMLInputElement>(null)`
- `useDeferredValue<string>(searchTerm)`
- `memo<ResponsiblePersonSelectProps>` on the export

CRITICAL: When renaming this file, simultaneously update the import in `src/features/prescriptions/components/PrescriptionForm.jsx`:
- Find: `from "./ResponsiblePersonSelect.jsx"`
- Replace with: `from "./ResponsiblePersonSelect"` (extension-free)
This prevents a TypeScript resolver error when PrescriptionForm is later converted in plan 02-06.

Rename to `.tsx`. Update PrescriptionForm.jsx import. Run `npx tsc --noEmit` → 0 errors.

After all 5 files, run final verification:
- `npx tsc --noEmit` → 0 errors
- `find src/features/employee-crud/components src/features/permits/components src/features/orders/components src/features/prescriptions/components -name "WorkerTrainingDownloadButton.jsx" -o -name "PermitStatusBadge.jsx" -o -name "PermitActions.jsx" -o -name "PermitsTable.jsx" -o -name "OrdersTable.jsx" -o -name "PrescriptionsTable.jsx" -o -name "ResponsiblePersonMultiSelect.jsx" -o -name "OrderForm.jsx" -o -name "PermitForm.jsx" -o -name "PermitsDashboard.jsx" -o -name "ResponsiblePersonSelect.jsx"` → returns empty (no .jsx files with these names remain)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    All 5 files converted to .tsx.
    `npx tsc --noEmit` exits 0.
    `grep -rn "as any" src/features/orders/components/ src/features/permits/components/ src/features/prescriptions/components/` returns no output for the 11 converted files.
    `grep -rn "from.*ResponsiblePersonSelect\.jsx" src/features/prescriptions/components/` returns no output (explicit extension removed from PrescriptionForm.jsx).
    `find ... -name "ResponsiblePersonSelect.jsx"` returns empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript compiler | Type checking enforced by tsc — no runtime behavior changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Realtime channel strings | accept | Channel strings live in REALTIME_CHANNELS constants, not in JSX files being converted. This plan does not touch service files or channel constants. |
| T-02-05-02 | Elevation of Privilege | Type assertions | mitigate | Prohibition on `as any` enforced by task acceptance criteria. Use `as SpecificType` only when unavoidable (Supabase generic boundaries). Verify with grep after each conversion. |
</threat_model>

<verification>
After all 11 conversions:
1. `npx tsc --noEmit` → exits 0
2. `find src/features -name "WorkerTrainingDownloadButton.jsx" -o -name "PermitStatusBadge.jsx" -o -name "PermitActions.jsx" -o -name "PermitsTable.jsx" -o -name "OrdersTable.jsx" -o -name "PrescriptionsTable.jsx" -o -name "ResponsiblePersonMultiSelect.jsx" -o -name "OrderForm.jsx" -o -name "PermitForm.jsx" -o -name "PermitsDashboard.jsx" -o -name "ResponsiblePersonSelect.jsx"` → returns empty
3. `grep -rn "as any" src/features/employee-crud/components/ src/features/permits/components/ src/features/orders/components/ src/features/prescriptions/components/` → returns no output
4. `grep -rn "from.*ResponsiblePersonSelect\.jsx"` → returns no output
</verification>

<success_criteria>
- All 11 .jsx files renamed to .tsx
- `npx tsc --noEmit` exits 0
- `grep -rn "as any" src/features/*/components/` returns 0 matches for any of the 11 converted files
- Every converted file has a named Props interface (grep for `interface.*Props` in each file)
- `memo<Props>(Component)` used for all memo-wrapped exports (not `memo(Component)`)
- Inline sub-components in PermitsDashboard have `React.ReactNode` children type
- Explicit `.jsx` extension in `PrescriptionForm.jsx` import of ResponsiblePersonSelect removed
- No changes to index.ts barrel files (barrel exports are extension-free already)
</success_criteria>

<output>
After completion, create `.planning/phases/02-type-safety/02-05-SUMMARY.md` documenting:
- All 11 files converted with their line counts
- Any TypeScript errors encountered and how they were resolved (especially recharts TooltipProps, memo generics, inline sub-component children)
- The RegistryEmployee type location found (ordersService.ts vs a shared types file)
- Output of final `npx tsc --noEmit` run
- Whether PrescriptionForm.jsx import was updated and confirmation grep
</output>
